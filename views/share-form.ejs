<html>
    <head>
        <title></title>
        <style>
            .logo {
                display: flex;
                gap: 1rem;
                align-items: center;
                position: fixed;
                top: 0;
                left: 0;
                opacity: 0.8;

                transform: translate(10%, -10%);
            }

            .logo h2 {
                font-size: 3rem;
            }

            body {
                height: 100vh;
                overflow: hidden;
            }

            .main {
                width: 100%;
                height: 100%;
                display: flex;
                justify-content: center;
                align-items: center;
            }

            .container {

                display: flex;
                flex-direction: column;
                position: relative;
                width: 60%;
                justify-content: center;
                gap: 2rem;
                padding: 4rem;
                margin: 2rem;
                border: 1px solid #ccc;
                border-radius: 1rem;
                box-shadow: 2px 2px 2px rgba(0, 0, 0, 0.5);

                margin-top: -200px;

            }

            form {
                display: flex;
                flex-direction: column;
                gap: 1rem;
                width: 60%;
                margin: auto;
            }

            .input {
                display: flex;
                gap: 2rem;
            }

            form button {
                width: 40%;
                margin: auto;
                padding: 0.25rem 2rem;
                background: none;
                border: 1px solid #ccc;

                transition: border 0.3s;

            }

            form button:hover {

                border: 1px solid rgba(0, 0, 0, 1);
                cursor: pointer;

            }

            .container h2 {

                position: absolute;
                top: 0;
                left: 0;
                transform: translate(10%, -175%);

            }
        </style>
    </head>
    <body>
        <div class="logo">
            <h2>File Dookie </h2>
            <img src="/assets/icon.png" height="50px" width="50px" alt="Logo">
        </div>
        <div class="main">
            <div class="container">
                <h2>Sharing folder <%= folder.name %></h2>
                <form action="/folders/<%= folder.id %>/share" method="post">
                    <div class="input">
                        <label for="duration">Share for (e.g., 1d, 10d):</label>
                        <input type="text" name="duration" id="duration" required>
                    </div>
                    <button type="submit">Share Folder</button>
                </form>
            </div>
        </div>

    </body>
</html>